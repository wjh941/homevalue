# 实验台账(EXPERIMENTS)

> 边做边记的实验日志:每次训练一行,来源于 reports/metrics_log.jsonl(自动追加)。
> 这份文件本身就是实验思维的证据:同一口径下横向对比,记录参数与结论。

## 实验环境与口径

- 数据:链家公开快照 35 期(北京,2022-09 至 2024-09 月度粒度)+ 上海/广州/深圳各 1 期
- 规模:原始 1,861,000 行 -> 清洗 1,509,593 行 -> 唯一房源 294,740 套(北京 287,105 套用于建模)
- 预测目标:unit_price(元/平米),模型在 log 空间训练,指标一律换算回原尺度
- 评估:随机 80/20 切分(seed=42),训练集内 5 折 CV,留出集终评
- 指标:MAE / RMSE / MAPE / R²(原尺度,元/平米)

## 第一轮:district 修复前(2026-09-23 03:12)

| exp | 模型 | 特征 | 留出集 MAE | MAPE | R² |
|---|---|---|---|---|---|
| exp001 | LinearRegression(OHE 区/装修) | 15 | 12,325 | 17.45% | 0.689 |
| exp002 | LightGBM 默认参数 | 15 | 8,903 | 12.44% | 0.824 |
| exp003 | LightGBM + 商圈特征 | 16 | **7,144** | 9.92% | 0.887 |

## 第二轮:district 白名单规整 + 商圈/小区两轮多数票修复后(03:17)

| exp | 模型 | 特征 | 参数 | 留出集 MAE | MAPE | R² | 80%区间覆盖率 |
|---|---|---|---|---|---|---|---|
| exp001 | LinearRegression | 15 | - | 14,909 | 21.38% | 0.570 | - |
| exp002 | LightGBM | 15 | 默认 | 11,726 | 16.52% | 0.716 | - |
| exp003 | LightGBM | 16 | 默认 | 7,179 | 9.97% | 0.886 | 77.4% |
| exp004 | LightGBM | 16 | num_leaves=127, lr=0.05, min_child_samples=40 | 6,824 | 9.48% | 0.896 | 76.1% |
| exp005 | LightGBM + **小区/商圈目标编码** | 19 | 同 exp004 | **5,641** | **7.85%** | **0.926** | 75.3% |
| exp006 | 同 exp005,**数据扩到 35 期快照** | 19 | 同 exp004 | 5,743 | 8.29% | 0.924 | **76.7%** |

5 折 CV(exp005):MAE 5,820 ± 13;CV(exp006,训练 229,635 / 测试 57,470):MAE 5,845。

## 结论(面试可讲的重点)

1. **最大单项增量是商圈特征**:加入 bizcircle 使 MAE 从 ~11.7k 降到 ~7.2k(-20% 相对基线),
   区位粒度对房价预测的边际信息量远大于装修/朝向等属性特征。
2. **数据规整让"只看区"的模型变差了**(exp002: 8,903 -> 11,726):因为脏的 district 值里
   暗藏商圈信息,修复后这部分"走私信号"消失;只有配上商圈特征(exp003/004),规整后的
   数据才达到最优。教训:**数据清洗要与特征工程联动评估,清洗前的"高分"可能是脏值红利。**
3. **调参收益有限但真实**:num_leaves 127 + lr 0.05 相对默认值 MAE -5%(7,179 -> 6,824),
   12 组网格 3 折 CV 用时约 2.5 分钟,收益/成本比可接受。
4. **80% 分位区间的实测覆盖率 75.3%**(exp005),略低于名义水平但足够诚实,可直接支撑 A/B 实验
   的"区间展示"策略(见 experiments/ab_design.md)。
5. **小区级目标编码是目前最大单项增量**:exp004 -> exp005,留出集 MAE 6,824 -> 5,641(**-17.3%**),
   远超调参收益(-5%)。关键工程点:CV 在**每折内部**拟合 TE(防泄漏),线上未见小区回落到全局均价
   先验(k=20 平滑);平滑系数靠直觉+验证集选定。
6. **数据扩量的正确预期**:35 期快照让房源池翻倍(148k -> 287k)、调价事件 3.5 倍(14k -> 50k)、
   趋势从 5 点变 35 点;但 exp006 的 MAE(5,743)与 exp005(5,641)基本持平——因为评估集也变难了
   (长尾小区、跨两年房龄与市场环境)。**数据扩量的价值在"支撑更厚的分析与更稳的区间"(覆盖率
   75.3% -> 76.7%),不在榜面指标**;这个判断本身就是面试素材。

## 线上服务实验记录

- API 压测 exp005(400 请求 / 8 并发):0 错误,P50 457ms,**P95 710ms**,吞吐 16.3 RPS
- 单并发(100 请求):P50 189ms,P95 264ms —— 单次预测 = 3 个模型推理(p50/p10/p90)+ TE 查表 + 可比数据 SQL
- A/B 设计仿真:required_n_per_arm(0.10, 0.02, 0.05, 0.80) = **3,841/臂**;
  蒙特卡洛 1,000 次:经验功效 81.1%,假阳性率 4.0%,分流均衡 49.9%

## 待做

- [ ] 小区级 target encoding(目前只到商圈粒度)
- [ ] SHAP 可解释性报告
- [ ] 空间特征:经纬度 + 距地铁/商圈 POI
- [ ] 每月自动追加快照并重训(CI 定时任务)
